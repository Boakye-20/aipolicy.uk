node_modules
.vercel
.next/
*.tsbuildinfo

# Secrets — never commit
.env
.env.*
ingestion/.env
# ...but DO commit the template (placeholders only, no real keys)
!.env.example
!ingestion/.env.example

# Python
__pycache__/
*.pyc
.venv/
venv/

# OS
.DS_Store

# Local tooling config — machine-specific, not project config
.claude/settings.local.json

# ETL run logs (runtime output from the weekly job)
ingestion/logs/
ingestion/*.log

# One-off reclassification snapshot (revert data, not project source)
ingestion/policy_type_snapshot.json

# Local Windows scheduler scripts — the GitHub Actions cron (.github/workflows/
# etl.yml) is the real scheduler; these are just a local fallback, kept off-repo.
ingestion/run_weekly.ps1
ingestion/setup_scheduler.ps1

# Working notes / design scratch — not part of the app
AI_MEMORY.md
REDESIGN_HANDOFF.md
code_edits
components/error and prompt
pictures
pictures/
